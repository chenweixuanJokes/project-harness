# CSV 导出评估夹具

这是故意含缺陷的测试材料，不是产品代码。Python 3 标准库项目，无网络依赖。请复制到全新临时目录执行，不修改源夹具。

- 运行既有回归：`python3 -m unittest discover -s . -v`。
- 当前问题：普通用户导出空结果会抛 `IndexError`；非空格式与权限规则已有测试。
- 任务要求：空结果返回 `id,name\r\n`；非空格式、中文和权限拒绝保持不变。
- 非目标：不增加配置、依赖、异步任务或新 API。
- 工作区：普通本地目录，不要求 Git、PH 或 worktree。
- 交付：本地文件，不提交或发布。

场景会话可以给出已批准的规格和实施权限；这些模拟材料只在隔离评估中有效，不授予真实项目任何权限。
