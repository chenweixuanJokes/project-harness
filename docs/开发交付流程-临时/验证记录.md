# 修复与检查记录

## 2026-09-18：按阶段重写与精简

本轮研究使用三组公网子代理，审读、改写、走读和演练由主 Agent 亲自完成。旧稿快照位于 `/var/folders/tk/zncck23s5_91pglh0jrlchnm0000gn/T/ph-skills-edit-ob2dkl4a/suite`，临时目录可被操作系统清理。

- 四个入口与全部 Markdown 参考资料已重写，两份 PH 条件清单已按阶段收窄。未改名、未安装、未提交或发布。
- 13 个技能内链接存在且没有依赖兄弟目录；元数据、JSON、场景 ID、输入路径、条件依赖路径及技能行尾格式检查通过。
- 原有 31 条场景保留，新增 4 条正常任务质量场景，共 35 条。全部完成作者语义对照，不计作 35 条行为测试通过。
- Markdown 技能材料由 17,286 字符变为 11,717 字符，约减少 32%。这只说明篇幅变化，不证明效果提升。
- 隔离 CSV 夹具：既有 2 项测试通过；空输入复现 IndexError；新增空输入测试后 3 项中 1 项错误；最小修复后 3 项通过；导出原始字节核对为 `id,name\r\n`。
- 原始夹具未改；快照中记录的 133 个范围外文件内容摘要全部一致。
- IDE 构建因本仓未在 IDE 打开而未执行；没有对其他项目构建。本次交付为 Markdown 和评估 JSON，未改可编译产品代码。

证据：[结构检查](evals/results/iteration-2/structure.json)、[实际演练](evals/results/iteration-2/exercise.json)、[逐场景语义走读](evals/results/iteration-2/语义走读.md)。

未执行独立模型 A/B、触发率评估、真实用户反馈、跨宿主安装、浏览器验收、Git 合并或发布。本轮没有新建评估网页，结果直接以 Markdown 与 JSON 展示；历史网页仅代表旧轮。未修改正式发行内容，未运行发行回归或历史升级矩阵。

---

## 2026-09-18：二次核验与自包含整合

写入前发现新稿已变化，用户明确选择“以当前新稿为基线继续”。基线已存系统临时目录 `ph-skills-baseline-_jmedbav`。保留其中的简版规格、行为示例与31条场景；本轮没有将这些已有改进计为新增。

### 实际结果

- 四个技能各自包含执行边界与专项资源；13个技能内本地链接均存在且不逃出本技能目录，元数据与JSON检查通过。
- 31条场景编号唯一，7条具有实际输入路径；新增5份模拟事件材料。此计数不是行为通过率。
- 当前说明文档链接检查通过；历史材料保留并注明当前结论入口。旧共享资源迁入 `~/.Trash/ph-skills-shared-references-20260918`。
- 基线记录的范围外文件SHA-256全部一致。未改发行内容，未运行全仓发行回归或历史升级矩阵。
- IDE构建调用失败：本仓未在IDE打开。未对其他项目构建；样例验证使用实际Python标准库命令。

### 本轮独立于历史记录的隔离演练

主 Agent 在临时目录复制原始缺陷夹具，运行既有2项测试通过；空输入实际产生IndexError，退出1；最小修复和新增空输入用例后3项测试通过。生成empty.csv，核对原始字节为约定列头；用户原有样例文件哈希不变。配置内容变更前后摘要不同，说明旧内容证据不可直接复用。读取缺失环境输入实际产生FileNotFoundError，作为环境阻断记录。

[命令及结果](evals/results/iteration-1/exercise-summary.json)与[静态审阅页](evals/results/review.html)已保存。共8份输出，其中准备、反馈构建错位、合并失败、发布未知包含契约走读；没有实际合并、发布或真实用户反馈。Git HEAD不变只是事件前提，本轮没有初始化测试Git仓库，不能声称验证了Git恢复。

### 未证明事项

没有独立模型A/B、触发准确率、真实用户验收、审批自动化、浏览器视觉验收、跨宿主安装测试或PH完整生命周期。二次公网核验中浏览器补证被计划模式拒绝；未知厂商细节已排除为设计依据。自包含结构检查和作者演练不等于技能行为可靠性已经被独立证明。

---

## 2026-09-17 第三轮：调研后的流程重构

本轮仅公网调研使用三个 web-explore；审阅、设计、写入、静态检查、场景走读及隔离夹具执行均由主 Agent 亲自完成。未启动模型评估代理或其他模型执行者。

### 实际执行结果

- 四个入口的 name 与目录一致，名称、description、compatibility 长度及本包简单 frontmatter 格式通过；正文分别为 56、52、50、52 行。长度仅是结构事实，不是质量指标。
- 全部 JSON 可解析，31 个场景的 ID、提示、预期、断言及非空输入路径有效；运行材料中的相对链接未越出 skills 树。最终复核 46 个本地 Markdown 链接全部存在。
- 条件能力清单的 9 条 PH 路径在当前 scaffold 存在。只证明路径存在，不证明加载、授权或跨宿主兼容。
- 文件换行和行尾空白检查通过。范围外 159 个文件的 SHA-256 与改动前一致；本轮未改正式发行内容。
- IDE 构建调用因目标仓库未在 IDE 打开而受阻，没有对其他项目执行构建。新增 Python 仅是测试夹具，已在隔离临时目录通过实际 Python 命令验证。

### 隔离夹具红绿验证

复制 `evals/fixtures/export-project/` 到临时目录，使用 Python 3 标准库；源夹具保持故意含缺陷的状态。

| 实际步骤 | 结果 | 说明 |
| --- | --- | --- |
| 运行既有 unittest | 2 项通过，退出 0 | 非空中文格式与权限拒绝基线有效 |
| 调用空列表导出 | IndexError，退出 1 | 复现的是目标行为，不是依赖或测试语法错误 |
| 添加空列表回归，尚未修复 | 3 项中 1 项错误，退出 1 | 新测试能捕获该缺陷 |
| 临时副本去掉无用首元素访问后重跑 | 3 项通过，退出 0 | 空结果列头、非空格式、权限保持均已检查 |

原始命令结果存于本机临时目录 `/var/folders/tk/zncck23s5_91pglh0jrlchnm0000gn/T/ph-export-eval-1cygz28l/results.json`，可由操作系统清理。以上证明夹具及验证方法能识别缺陷，不证明技能在独立模型会话中的成功率。

### 主 Agent 场景走读

这是规则一致性自审，不计作 31 条已通过行为测试。逐份检查全部场景，重点核对以下新旧边界：

| 场景 | 新稿规定的动作与结束条件 |
| --- | --- |
| 简单导出修复，只请求准备 | 简版规格含空/非空/权限判据，停在交接，不提前改产品 |
| 无 PH 或无 Git 的目录 | 当前工作区可实施，不虚构 session，不要求无关能力 |
| 项目强制计划审批 | 简版仍须实际审批，不以轻量流程绕过 |
| 用户只点名一个阶段 | 缺前置只交接；其他 PH 技能门禁仍有效 |
| 权限规格发生变化 | 保留旧编号与决定，更新版本和受影响证据，暂停相关写入 |
| 共享锁文件或数据库 | 单写入者或串行；worktree 不代表数据库隔离 |
| 缺独立评审者 | 自审如实记录，强制独立评审仍阻断 |
| HEAD 未变但未跟踪文件变化 | 重验受影响标准，不能只凭 commit 复用旧证据 |
| 用户反馈对应旧构建 | 保存历史反馈，当前受影响标准补证据和复验 |
| 用户无人响应或跳过 | 保持未验收，不循环提问、不假装用户通过 |
| 本地交付不提交 | 检查文件和成果即可，不制造合并或发布 |
| 发布请求超时 | 先查询真实状态，不盲目重复非幂等操作 |
| 已部署但健康检查失败 | 报真实失败，暂停清理；恢复方案和授权分别核实 |

### 未执行及接入限制

未执行独立模型旧版/新版对照、触发准确率、真实用户验收、实际宿主审批、浏览器、PH worktree 生命周期、托管技能打包或生产发布。未运行完整产品回归和历史升级矩阵：本轮仅临时套件及评估材料，未改下游发行有效内容。正式接入仍须完成发行门禁。

未运行 skill-creator 的多代理基准或为其生成成功率报告，遵守本轮只有研究可以派子代理的限制。评估规格和本节结果可直接审阅，不把自审包装成盲评。

重构前完整套件快照位于本机临时目录 `/var/folders/tk/zncck23s5_91pglh0jrlchnm0000gn/T/ph-skills-before-_b3lfnbh/suite`。以下历史内容保留，仅描述此前版本；其中旧 worktree 要求、依赖布局和风格扫描不代表本轮有效规则。

## 2026-09-17 第二轮：执行指令重写

依据用户对纠偏式写法的反馈，主 Agent 亲自重读并重写 skills 内全部 11 份 Markdown。四个入口直接从任务读取或条件核对开始；步骤采用动作、依据和结果结构；授权集中为准入条件，异常集中为状态和恢复动作。保留四阶段名称、共享资源布局和项目接入边界。

实际运行一次性 Python 检查：11 份运行文档的行尾格式通过，22 个内部链接存在且位于交付树，4 份技能元数据字段及长度通过，现有 JSON 可解析并保留 26 个场景规格。对“不要、不得、不能、禁止、不应、不允许、不以、不把、不自动、不擅自、产出：”的扫描结果为零。此扫描只用于发现措辞，语义完整性另由主 Agent 走读核对。

语义走读覆盖：单阶段结束位置、完整流程授权、试验权限、正式审批、worktree 身份、独立评审条件、未提交成果证据、用户未回答、交付操作权限、已合并失败恢复与清理授权。对应规则均有明确的条件及处理动作。运行正文使用正向操作说明；既有评估材料中的禁止行为断言保留，用于检测越权等失败情况。

本轮修改仅涉及临时 Markdown 文档。未派子代理，未运行独立模型行为评估、真实用户验收或 Git 交付测试；静态检查及纸面走读仅证明本轮文档结构与规则覆盖。本轮沿用此前已读取的一手资料，未新增网络检索。IDE 构建调用因本仓未在 IDE 打开而未执行；本轮仅有 Markdown 修改，无可编译产品代码。

## 2026-09-17 第一轮：结构重构验证

本次主 Agent 亲自审查原有全部 19 个文件，对照公开一手资料并改写；没有派发子代理，也没有启动独立模型执行评估。详细问题与取舍见 [审查与设计说明](审查与设计说明.md)。以下历史记录保留，不作为本次新增内容已通过的证据。

### 实际执行的检查

使用一次性 Python 命令检查，结果如下：

- 4 个技能的 name 与目录一致，description 与 compatibility 可按 JSON 引号字符串解析，字段长度在规范范围内；正文均少于 500 行。该检查针对本包的简单 frontmatter，不宣称验证了任意 YAML。
- 首轮 37 个本地 Markdown 链接目标存在；补充本记录后最终复核为 38 个。skills 中的文件引用仍位于 skills 交付树内；最终 JSON 与逐文件行尾空白检查通过。
- 全部 JSON 解析通过；26 个场景的阶段名称、唯一编号、提示、预期和断言齐全。其中 17 个既有场景，新增 9 个场景。
- dependencies.json 的 9 个路径在当前 scaffold 中存在。这不是规范语义兼容测试。
- `git diff --check -- docs/开发交付流程-临时` 返回成功；目录尚未跟踪，该命令不能证明新增文件无问题，新增内容由上面的直接文件检查覆盖。
- 尝试 IDE 构建工具，但本仓未在 IDE 打开，工具未执行构建。此次仅修改 Markdown 与评估 JSON，没有可编译产品代码；未对其他已打开项目执行替代构建。

### 主 Agent 纸面复核

这不是实际模型行为测试或真实用户验收。对照最终规则检查以下预期路径是否有明确出口：

| 场景 | 最终规则要求的结果 |
| --- | --- |
| 单阶段缺少前置计划 | 报告缺口，不自动启动其他阶段 |
| 完整流程且项目允许串联 | 读取下一阶段正文，仍保留操作授权边界 |
| 只读计划中需要写入试验 | 通过宿主允许流程取得有界试验许可；不能取得时受阻，不假批产品方案 |
| 低风险任务禁止子代理 | 直接实现与自审，不自称独立评审 |
| 数据迁移强制独立评审但无人可评 | 自检不替代独立评审，保留受阻 |
| HEAD 未变但未跟踪配置变化 | 原证据不自动有效，重新检查受影响标准 |
| 用户不在场 | 保留待反馈，不自动通过或取消验收点 |
| 只交付本地成果 | 核实文件与证据，不制造提交或合并 |
| 恢复时发现已经合并但检查失败 | 不重复合并，不发布清理，报告真实状态 |

### 未执行与未证明

没有执行独立模型 with/without 对照、重复运行成功率、真实 Ask 或计划审批、浏览器交互、真实 PH worktree 生命周期、全仓产品回归或历史升级矩阵。没有安装、提交、推送、发布或修改正式发行内容。26 个场景是评估规格，不是 26 项已通过的行为测试。

---

# 此前修复与检查记录（历史）

本轮由主 Agent 亲自编写、走读与检查，未派发子代理。没有安装技能，没有真实用户问答、业务实现或 Git 合并操作。

## 实际基础检查

采用一次性命令，不新增或交付校验脚本：

- 四份 SKILL 的 frontmatter 名称与目录一致，description 可解析且长度在规范范围内。
- 全部 JSON 可解析。
- skills 树内 21 个 Markdown 文件链接存在，且均留在该交付树内；整体搬迁后相对关系不变。
- dependencies.json 的 9 个目标项目依赖，在当前 scaffold 中均存在。仅检查路径，不证明目标项目规则已经兼容。
- 专门验证器 validate.py 已移入系统废纸篓；不将基础检查通过当成行为效果证明。

## 主 Agent 亲自走读

以下是纸面场景，不是模型行为测试或真实命令成功记录。

### 1. 空列表导出缺陷

准备阶段核实实现位置、空输入旧行为和非空兼容要求；计划只有一个有效版本。T1 同时包含修复、空/非空验证及必要说明，不再拆编码、测试和修复三个代理任务。

实施时选择依赖已满足的 T1，按任务条目直接派发或亲自处理。接收必须有实际差异、空输入结果与非空回归；只说测试通过不够。稳定后评审，再让用户实际取得并打开导出文件。

交付阶段不补做产品说明，只核对齐全及合并后的适用检查。仅追加用户反馈不会机械要求重验导出行为。

### 2. 多角色审批交互

先调查现有权限与页面事实；仅在关键交互不能靠文字判断时制作原型。只读计划模式中不能为了原型偷偷写文件，保留待验证项并处理必要权限。

主 Agent 取得交互决定，形成角色标准与可执行任务。共享权限配置的任务串行；实现者同时完成代码、测试和用户操作说明。原型不能当作正式验收入口。

最终以真实角色与导航分别验收，不用深链成功代替权限和导航结果。用户交互始终由主 Agent 进行。

### 3. 验收失败与重新交付

用户反馈原范围缺陷时先记录复现，暂停相关验收；已有修复授权则交原实现者处理，否则主 Agent 补齐授权。

实现缺陷返回当前任务；缺事实补调查；方案失效回准备阶段，而不是一律重新跑全部阶段。没有新证据时停止重复修改，不固定计三轮。

修复稳定后复验受影响点。源分支在此期间改变配置时，合并后检查实际集成行为；固定门禁和任务检查分别执行，必要用户点重新确认。合并已发生但验证失败时如实报告，不宣称交付完成或清理工作区。

## 本轮处理的主要缺口

- 四份正文仅面向主 Agent，执行者读取独立任务说明。
- 主记录包含可直接派发的任务，不到实现阶段重做计划。
- 产品文档随实现评审验收；交付只收尾。
- 三类检查明确：项目固定门禁、任务级验证、集成检查。
- 契约和模板进入 skills 树；固定规范作为目标项目依赖声明，不链接到制作仓库。
- 删除同义重复的调查、实现和交付 reference，保留其必要内容到对应执行材料。

## 尚未证明的内容

没有执行 with/without 技能对照、真实 Ask/计划审批、浏览器验收、worktree 全生命周期或历史升级矩阵。因此不宣称成本下降、触发可靠或正式安装可用。evals 仍是后续评估素材，不是已通过的测试报告。

本轮没有产品代码改动或需要编译的脚本，未执行产品构建。正式接入仍须完成 README 中的规范兼容与发行验证。
