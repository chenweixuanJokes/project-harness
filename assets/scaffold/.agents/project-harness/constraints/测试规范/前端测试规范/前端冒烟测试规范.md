# 前端冒烟测试规范

Last verified: <填写：YYYY-MM-DD>

使用时机：写或执行前端冒烟剧本、配置浏览器测试账户与依赖服务、登记冒烟证据时读本文。

> 共用字段与场景类型在 [用例规范](../用例规范.md)；影响范围分档与回归组合在 [门禁规范](../门禁规范.md)；命令的单一源在 [自测规范](../../前端规范/自测规范.md)。本仓库无前端时，本文各节标不适用。

## 1. 载体与格式

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 剧本载体 | `<填写：.md 交互剧本 / 自动化脚本 / 两者并用>` | `<填写：样例路径>` |
| 剧本位置 | 放 [前端测试用例](./前端测试用例/_模板.md) 目录（只放冒烟用例），一剧本一文件；自动化剧本也可放仓库测试目录，在测试用例目录登记入口 | `<填写：实际位置>` |
| 执行方式 | `<填写：agent 驱动浏览器执行 / 脚本执行；工具不限还是固定>` | `<填写>` |

剧本字段沿用 [用例规范](../用例规范.md) 共用字段；「证据记录」段执行后必须回填，不回填视为未跑。

## 2. 环境与登录态

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 依赖服务 | `<填写：dev server、本地后端、网关；启动方式引用前端或后端构建规范>` | `<填写>` |
| 测试账户 | `<填写：本地账户配置位置与格式；只写位置，不写明文口令>` | `<填写>` |
| 登录态准备 | `<填写：如何取得登录态（账户配置 / 模拟登录工具位置）>` | `<填写>` |

禁止用生产凭证跑本地冒烟；密钥与账户口令不入库，见 [安全规范](../../工程规范/安全规范.md)。

## 3. 执行与证据

- 每次执行后填写剧本内「证据记录」：日期、结果、关键截图或报告路径；没有证据的「我跑过了」不算通过。
- 报告与产物目录：`<填写：如 tests/reports/；无则写无>`。
- 缺陷先用同一剧本复现，修复后再用同一剧本验证；断言失败即失败，不得跳过。

## 4. 页面改动时的对应用例

改路由、页面、跨页面可复用模块必须同步对应剧本；删除功能时同步移除或标注其剧本。剧本登记与状态在 [前端测试用例](./前端测试用例/_模板.md) 目录维护。
