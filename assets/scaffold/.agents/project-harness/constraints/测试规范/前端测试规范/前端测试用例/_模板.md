<!-- 使用：复制本模板到本目录，一冒烟用例一文件，命名 <主题>.md（如 login-modal.md）。本目录只放冒烟用例；单元等自动化用例留在各端测试目录，不在 constraints/ 登记。状态在 已实现 / 待实现 / 未执行 中选一，待实现与未执行不得出现在通过报告里。 -->

# <冒烟用例标题>

| 字段 | 内容 |
| --- | --- |
| 用例 ID | `<稳定、仓库内唯一；已有编号沿用>` |
| 覆盖功能 | `<指向功能地图的功能名或路由 / 接口>` |
| 状态 | 已实现 / 待实现 / 未执行 |
| 剧本源码 | `<自动化剧本 / 脚本的仓库路径；纯文档剧本写「本文」>` |
| 环境 | `<本地 / 需依赖的服务；指向开发指南>` |

## 前置

`<账号、开关、服务是否已起、特性开关>`

## 测试数据

`<构造方式与隔离范围；不写真实密钥或生产数据>`

## 步骤

`<可重复的操作或调用；文档剧本写清每步交互与期望>`

## 断言

`<可观察的期望；失败即失败>`

## 清理

`<数据、文件、会话如何收回；没有副作用写「无」>`

## 证据记录

`<每次执行后回填：日期、结果、报告路径或截图位置>`
