# 后端规范：构建规范

Last verified: <填写：YYYY-MM-DD>

使用时机：改后端构建命令或脚本、调整产物形态与环境注入、本地启动服务、用产物在本地联调或排查产物过期（stale）时读本文。

> 只登记本仓库后端脚本与配置能核实的本地事实。CI 流水线、部署、发布、回滚、监控等远程事实看代码无法推断，**不在默认模板范围**：项目实际需要登记时另建专文并在文首写明使用时机（宪法导航由 PH init / 升级刷新），不从模板预填。本文是文档约定，**填写或补全本文不等于授权真实部署、推送或发送通知**。
>
> 逐步上手见 [开发指南](../../documents/开发指南.md)；构建前后的验证要求按 [测试规范：门禁规范](../测试规范/门禁规范.md) 执行。

## 1. 构建命令

| 项 | 本仓库值 |
| --- | --- |
| 构建（全量） | `<填写：真实脚本；未执行须标明>` |
| 构建（增量 / 开发） | `<填写或无>` |
| 构建前提 | `<填写：JDK / 运行时版本来源、锁文件或依赖清单、是否先清理>` |
| 清理 | `<填写：clean 脚本或需要手动删除的目录>` |

## 2. 产物

| 项 | 约定 |
| --- | --- |
| 产物路径与形态 | `<填写：target/*.jar / 二进制 / 镜像构建脚本与 tag 规则>` |
| 环境注入 | `<填写：构建期变量名与来源；密钥走配置通道不进产物>` |
| 成功判据 | `<填写：怎么判断构建成功且产物完整（产物清单、校验命令）>` |
| 可复现 | `<填写：是否要求干净构建后使用；是否允许复用旧产物>` |

发布或联调前按开发指南重建产物，避免 stale class。产物校验标准写在这里，不要只写在聊天记录里。

## 3. 本地启动

| 主题 | 约定 | 证据 |
| --- | --- | --- |
| 启动方式 | `<填写：profile / 环境变量；禁止用生产配置则写明>` | `<填写>` |

## 4. 产物本地运行

| 主题 | 约定 | 证据 |
| --- | --- | --- |
| 用产物在本地跑 | `<填写：如何用构建产物在本地跑（如本地起 jar）；无则写无>` | `<填写>` |
