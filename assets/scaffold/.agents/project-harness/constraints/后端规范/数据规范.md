# 后端规范：数据规范

Last verified: <填写：YYYY-MM-DD>

使用时机：建表改表、写 SQL 与 ORM、动缓存或消息、执行与回滚 Schema 迁移时读本文。

> 存储选型与连接配置的事实登记在本文；密钥保管在 [安全规范](../工程规范/安全规范.md)；事务与幂等的编码约定在 [技术规范](./技术规范.md) §4。

填写纪律：库 / 缓存 / 消息必须是本仓库真实依赖；无则写「无」，不预填。

## 1. 存储清单

| 能力 | 用途 | 连接与版本 | 证据 |
| --- | --- | --- | --- |
| 数据库 | `<填写或无>` | `<填写>` | `<填写：配置 / 镜像>` |
| 缓存 | `<填写或无>` | `<填写>` | `<填写>` |
| 消息 | `<填写或无>` | `<填写>` | `<填写>` |

## 2. 建表与索引

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 表与字段命名 | `<填写>` | `<填写：样例表>` |
| 主键与必要列 | `<填写：主键形态、审计列>` | `<填写>` |
| 索引依据 | `<填写：先查再建、慢查阈值>` | `<填写>` |
| 禁止事项 | `<填写：如外键约束取舍、字段类型禁例>` | `<填写>` |

## 3. SQL 与 ORM

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 查询写法 | `<填写：ORM / SQL 分界、大查询分页>` | `<填写>` |
| 禁止事项 | `<填写：SELECT *、N+1、大事务>` | `<填写>` |
| 慢查治理 | `<填写：阈值、看哪里>` | `<填写>` |

## 4. Schema 迁移

通用规则：Schema 变更走迁移脚本，不改已应用的历史迁移。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 工具与目录 | `<填写：Flyway / Liquibase / 手写脚本>` | `<填写：迁移目录>` |
| 命名与顺序 | `<填写>` | `<填写>` |
| 回滚策略 | `<填写：可回滚范围、不可回滚时的流程>` | `<填写>` |
| 大表变更 | `<填写：锁表策略；无则不适用>` | `<填写>` |

## 5. 缓存与消息约定

| 项 | 约定 | 证据 |
| --- | --- | --- |
| Key 规范 | `<填写：前缀、分隔、默认 TTL>` | `<填写>` |
| 一致性 | `<填写：先写库还是先删缓存、失效策略>` | `<填写>` |
| 幂等消费 | `<填写：重复消息怎么处理>` | `<填写>` |
| 顺序与重试 | `<填写：乱序容忍、死信去向>` | `<填写>` |
