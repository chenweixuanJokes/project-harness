# 后端规范：技术规范

Last verified: <填写：YYYY-MM-DD>

使用时机：确认语言 / 运行时 / 框架版本、动分层与模块边界、接入共享能力、写或评审后端代码、新增或修改对外接口时读本文。

> 本文统一登记后端技术栈的事实与约定：选型与运行时、分层、公共组件、编码、接口契约。存储与建表在 [数据规范](./数据规范.md)；各层测试命令在 [自测规范](./自测规范.md)；本地构建、启动与产物在 [构建规范](./构建规范.md)。密钥与权限边界只在 [安全规范](../工程规范/安全规范.md)；跨端测试门禁只在 [测试规范：门禁规范](../测试规范/门禁规范.md)。

填写纪律：语言、框架、中间件必须能在依赖声明、构建文件或启动配置里核实；核不到就写「无」或「不适用」，不预填具体厂商或邻项目版本。命名与包结构跟仓库现状；注释写 why，不写 what。本仓库无后端时，本文各节标不适用。

## 1. 语言与框架

| 项 | 值 | 证据 |
| --- | --- | --- |
| 语言 / 运行时 | `<填写>` | `<填写：构建文件、锁文件、运行镜像>` |
| 框架与版本 | `<填写>` | `<填写>` |
| 应用入口 | `<填写>` | `<填写>` |
| 代码生成 / 脚手架 | `<填写：可手改 vs 生成物；无则不适用>` | `<填写>` |

## 2. 分层与模块

分层按仓库现状登记，不从邻项目照抄分层名。通用规则：入口适配器、领域规则、持久化、对外客户端分层；HTTP / RPC 细节不渗进领域。生成物与手改文件分开，不改生成物凑功能。

| 模块 | 职责 | 代码路径 | 不跨越的边界 |
| --- | --- | --- | --- |
| `<填写>` |  |  |  |

分层与依赖方向：`<填写：入口、应用、领域、持久化、对外客户端各在哪、谁能依赖谁>`。证据：`<填写>`。

## 3. 公共组件

只登记本仓库实际调用的共享能力；没有该能力的行写「无」，不删表格。需要调用平台或其它内部服务时：优先用已发布 SDK / 契约，禁止在业务模块手写重复客户端和镜像 DTO——除非本仓库明确没有 SDK。

Kong、Eureka、Apollo 以及同类产品：仅当依赖声明或运行配置证明在用时，写入「本仓库如何用」，不因常见而预装。

| 能力 | 是否使用 | 本仓库如何用 | 禁令 | 证据 |
| --- | --- | --- | --- | --- |
| 权限 / 身份服务 | `<填写：使用 / 无>` | `<填写或无>` | `<填写>` | `<填写>` |
| 网关 / 服务治理 | `<填写：使用 / 无>` | `<填写：若是 Kong 等须有依赖证据>` | 本地是否必须挂网关：`<填写>` | `<填写>` |
| 注册中心 | `<填写：使用 / 无>` | `<填写：若是 Eureka 等须有依赖证据>` | 本地调试是否允许注册：`<填写>` | `<填写>` |
| 配置中心 | `<填写：使用 / 无>` | `<填写：若是 Apollo 等须有依赖证据>` | 本地覆盖优先级：`<填写>` | `<填写>` |
| 其它 | `<填写或无>` | `<填写>` | `<填写>` | `<填写>` |

配置项只记名称与来源，不把真实值抄进文档；配置清单可落在 [开发指南](../../documents/开发指南.md)。

## 4. 编码

- 只修改当前任务必要的代码，不附带无关重构或格式化。
- 遵循现有命名、模块边界和代码风格。
- 清理本次修改造成的无用内容，原有无关问题只报告。
- 先明确可验证目标，再实现并自测；不以改变规则掩盖失败。


### 4.1 命名与包结构

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 命名 | `<填写：类 / 函数 / 变量、缩写禁例>` | `<填写：样例包路径>` |
| 包结构 | `<填写：按域 / 按层；新代码放哪>` | `<填写>` |

### 4.2 校验

通用规则：校验发生在边界；领域对象不接收未校验的外部输入。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 入参与路径校验 | `<填写：注解 / 过滤器 / 手写、拒绝时返回什么>` | `<填写>` |
| 权限校验 | `<填写：注解位置、默认拒绝还是默认放行>` | `<填写>` |
| 内部调用信任边界 | `<填写：内部接口是否免校验、依据>` | `<填写>` |

### 4.3 异常与错误模型

通用规则：对外错误有稳定码，不把堆栈或内部 SQL 回给调用方。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 错误模型 | `<填写：异常体系 / 错误码段、4xx 与 5xx 划分>` | `<填写>` |
| 错误码登记 | `<填写：码表位置、新增码的流程>` | `<填写>` |
| 兜底 | `<填写：未预期异常的统一出口>` | `<填写：全局处理器路径>` |

### 4.4 日志与脱敏

通用规则：日志默认脱敏，禁止写密钥、cookie、完整证件号；关联 ID 仅在项目已有该机制时沿用同一套。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 级别使用 | `<填写：什么级别记什么>` | `<填写>` |
| 必带字段 | `<填写：traceId / 业务主键等>` | `<填写>` |
| 脱敏清单 | `<填写：哪些字段必须脱敏、怎么脱>` | `<填写>` |

### 4.5 事务与幂等

通用规则：写操作声明事务边界；跨库 / 跨服务不假装本地事务。可能被重试的写接口要有幂等键或自然幂等。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 事务边界 | `<填写：默认传播、禁止事项>` | `<填写>` |
| 幂等接口 | `<填写：哪些写接口、键从哪来>` | `<填写>` |

### 4.6 超时与重试

通用规则：超时与重试只放在出口，有上限和幂等前提，禁止对非幂等写无限重试。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 出口超时 | `<填写：HTTP / RPC / DB client 各自超时>` | `<填写：client 配置>` |
| 重试策略 | `<填写：次数、退避、对何种错误重试>` | `<填写>` |

## 5. 接口契约

契约形态与版本以仓库内契约文件为准；对不上就标「未核实」，不编造兼容规则。前端消费侧约定在 [前端规范：技术规范](../前端规范/技术规范.md) §4。改对外契约必须改调用方与用例。

### 5.1 契约形态与存放

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 形态 | `<填写：OpenAPI / proto / 代码即契约>` | `<填写：契约文件或生成目录>` |
| 生成物 | `<填写：生成流程、产物位置、可否手改>` | `<填写>` |
| 契约评审 | `<填写：新增接口是否过评审；无则写无>` | `<填写>` |

### 5.2 版本与兼容

通用规则：破坏性变更必须有弃期与迁移说明，不静默改字段语义。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 版本策略 | `<填写：URL 版本 / 头版本 / 不版本的理由>` | `<填写>` |
| 兼容规则 | `<填写：可加什么、不可改什么>` | `<填写>` |
| 弃用流程 | `<填写：弃用标记、公告渠道、下线周期>` | `<填写>` |

### 5.3 鉴权

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 鉴权位置 | `<填写：谁验（网关 / 过滤器 / 手写）>` | `<填写：过滤器 / 中间件路径>` |
| 令牌 | `<填写：放哪、有效期、刷新>` | `<填写>` |
| 匿名接口名单 | `<填写：显式列出或指向登记处>` | `<填写>` |

### 5.4 错误码与通用约定

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 对外错误码表 | `<填写：位置、与内部错误模型的映射>` | `<填写>` |
| 分页 / 列表 | `<填写：参数名、排序、默认页大小>` | `<填写>` |
| 长任务 / 异步 | `<填写：轮询还是回调；无则不适用>` | `<填写>` |

### 5.5 变更义务

新增或修改接口时：更新契约与码表 → 通知调用方（含前端）→ 同步接口用例（见 [自测规范](./自测规范.md)）。缺一步不算完成。
