# 后端规范：自测规范

Last verified: <填写：YYYY-MM-DD>

使用时机：功能完成要跑测试、核对各层命令与通过标准时读本文。

> 门禁与分档只在 [测试规范：门禁规范](../测试规范/门禁规范.md)；单元、冒烟与回归细则在 [后端单元测试规范](../测试规范/后端测试规范/后端单元测试规范.md)、[后端冒烟测试规范](../测试规范/后端测试规范/后端冒烟测试规范.md) 与 [后端回归测试规范](../测试规范/后端测试规范/后端回归测试规范.md)；共用字段与替身原则在 [用例规范](../测试规范/用例规范.md)；隔离与「重跑不算通过」只在 [稳定性规范](../测试规范/稳定性规范.md)。本地构建、起服务与跑产物在 [构建规范](./构建规范.md)。

填写纪律：没有脚本就写「无」或「待实现」；init 不代写测试实现，未执行不得标通过。本仓库无后端时，本文各节标不适用。

## 1. 自测命令

| 层 | 何时跑 | 命令 | 产物 / 证据 | 通过标准 |
| --- | --- | --- | --- | --- |
| 静态检查 | 合入前 | `<填写：已有 lint、格式与类型检查入口；没有则说明采用计划>` | `<填写：执行证据>` | 已采用检查全部通过 |
| 单元 | 功能做完；默认模块全量 | `<填写>` | `<填写>` | 全绿 |
| 接口 | 新增或修改 HTTP / RPC 契约 | `<填写>` | `<填写>` | 用例与契约同步 |
| 端到端 | 跨服务关键路径 | `<填写或无>` | `<填写>` | 选定范围全绿 |
| 冒烟 | 缺陷复现与修复验证 | `<填写：用例目录与跑法>` | `<填写>` | 同一用例先红后绿 |
| 回归 | 重构或合入共享基线 | 全量单元 + 全部冒烟（细则见 [后端回归测试规范](../测试规范/后端测试规范/后端回归测试规范.md)） | 同各层证据 | 任一层失败阻塞 |

新增接口必须同步用例；改接口必须改用例。重构不论看起来多等价，都按回归门禁。

禁令：`<填写：例如禁止生产库用于本地测试、禁止用生产配置启动>`。没有依据则写「未声明」。
