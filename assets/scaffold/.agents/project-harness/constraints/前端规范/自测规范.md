# 前端规范：自测规范

Last verified: <填写：YYYY-MM-DD>

使用时机：功能完成要跑测试、核对各层命令与通过标准、交付合入核对前先看本文。

> 门禁与分档只在 [测试规范：门禁规范](../测试规范/门禁规范.md)；单元、冒烟与回归细则在 [前端单元测试规范](../测试规范/前端测试规范/前端单元测试规范.md)、[前端冒烟测试规范](../测试规范/前端测试规范/前端冒烟测试规范.md) 与 [前端回归测试规范](../测试规范/前端测试规范/前端回归测试规范.md)；共用字段与替身原则在 [用例规范](../测试规范/用例规范.md)；隔离与「重跑不算通过」只在 [稳定性规范](../测试规范/稳定性规范.md)。本地构建、起服务与跑产物在 [构建规范](./构建规范.md)。

填写纪律：没有对应脚本就写「无」或「待实现」，不在 init 时编造测试实现，也不把未跑的命令标成已通过。本仓库无前端时，本文各节标不适用。

## 1. 自测命令

| 层 | 何时跑 | 命令 | 产物 / 证据 | 通过标准 |
| --- | --- | --- | --- | --- |
| 静态检查 | 合入前 | `<填写：已有 lint、格式与类型检查入口；没有则说明采用计划>` | `<填写：执行证据>` | 已采用检查全部通过 |
| 单元 | 功能做完；完成标准看测试规范 | `<填写：本仓库真实脚本>` | `<填写：报告目录或 CI job>` | 全绿 |
| 端到端 | 跨页关键路径变化 | `<填写或无>` | `<填写>` | 选定剧本全绿 |
| 冒烟 | 缺陷复现与修复；合入主分支前按范围 | `<填写：剧本目录与跑法>` | `<填写>` | 断言失败即失败 |
| 回归 | 重构或合入共享基线 | 全量单元 + 全部冒烟（细则见 [前端回归测试规范](../测试规范/前端测试规范/前端回归测试规范.md)） | 同各层证据 | 任一层失败阻塞 |

新增页面 / 路由 / 可复用模块必须同步用例；改页面或接口对接必须改用例。用例文件按 [前端测试用例](../测试规范/前端测试规范/前端测试用例/_模板.md) 目录约定登记，不在本文粘贴源码。

禁令：`<填写：例如禁止本地连生产 API、禁止用生产配置或生产数据启动>`。没有依据则写「未声明」。
