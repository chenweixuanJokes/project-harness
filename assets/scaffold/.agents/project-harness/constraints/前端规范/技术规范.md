# 前端规范：技术规范

Last verified: <填写：YYYY-MM-DD>

使用时机：确认运行时与框架版本、动目录边界与依赖方向、写或改组件与页面代码、对接后端接口契约时读本文。

> 本文统一登记前端技术栈的事实与约定：选型与运行时、目录结构、编码、接口对接。页面呈现与三态在 [样式规范](./样式规范.md)；各层测试命令在 [自测规范](./自测规范.md)；本地构建、启动与产物在 [构建规范](./构建规范.md)。跨端测试门禁只在 [测试规范：门禁规范](../测试规范/门禁规范.md)；共用安全边界（脱敏、环境隔离）只在 [安全规范](../工程规范/安全规范.md)。

填写纪律：项目事实必须能在本仓库（依赖声明、锁文件、源码路径、脚本）核实；核不到就写「不适用」或留 `<填写>`，不要从邻项目、本分发仓或网上教程填具体栈。命名、导入跟随本仓库已有写法；没有统一写法时先记现状，再决定是否收敛，不在补文档时改代码。本仓库无前端时，本文各节标不适用。 <!-- ph:placeholder-example -->

本仓库前端：`<填写：有 / 无。无则本文各节标不适用>`。

## 1. 运行时与框架

- 运行时与框架版本以锁文件 / 依赖声明为准，本文只记录已核实版本，不在特性分支顺手升主版本。
- 升级主版本、换构建工具或换状态方案属于架构决策，复制 [架构决策/_模板.md](../架构决策/_模板.md) 新建 `NNNN-短标题.md` 记录后再动，不在补文档时顺手做。

| 项 | 值 | 证据 |
| --- | --- | --- |
| 运行时 | `<填写：如 Node / 浏览器基线；无则不适用>` | `<填写：.nvmrc / engines / 构建配置路径>` |
| 框架与版本 | `<填写：名称 + 已锁定版本>` | `<填写：package.json / lock 路径>` |
| 包管理器与锁文件 | `<填写：npm / pnpm / yarn 与锁文件名>` | `<填写>` |
| 应用入口 | `<填写>` | `<填写>` |

### 1.1 性能底线

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 首屏与分包 | `<填写：路由级分包策略；无则不适用>` | `<填写>` |
| 图片与静态资源 | `<填写：懒加载、压缩、CDN>` | `<填写>` |
| 度量 | `<填写：用什么看包体与首屏；未建立则写未建立>` | `<填写>` |

## 2. 目录与结构

只列真实目录，不要按想象补齐；职责按现状登记，不在补文档时改代码。

### 2.1 目录边界

页面不拼环境地址、不直接持有未封装的鉴权头；依赖方向不能反向。

| 目录 | 职责 | 不放什么 |
| --- | --- | --- |
| `<填写>` |  |  |

依赖方向：`<填写：如 页面 → 组件 → 服务 → 工具，禁止反向导入与循环依赖；有 lint 规则则写规则名>`。证据：`<填写>`。

### 2.2 路由

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 定义位置 | `<填写>` | `<填写>` |
| 动态段与参数 | `<填写：命名与解析方式>` | `<填写>` |
| 守卫与鉴权跳转 | `<填写：谁拦、未登录去哪>` | `<填写>` |
| 与功能地图对应 | 路径名以本仓库路由定义为准 | `<填写：功能地图路径>` |

### 2.3 状态

通用规则：全局 / 页面 / 服务端状态分属不同位置；什么能进全局由本文登记，新类型先复制 [架构决策/_模板.md](../架构决策/_模板.md) 新建 `NNNN-短标题.md` 记录，再进全局。

| 档 | 放什么 | 用什么实现 | 证据 |
| --- | --- | --- | --- |
| 全局状态 | `<填写>` | `<填写>` | `<填写>` |
| 页面状态 | `<填写>` | `<填写>` | `<填写>` |
| 服务端状态 | `<填写：缓存 / 请求层管>` | `<填写>` | `<填写>` |

### 2.4 请求层

| 项 | 约定 | 证据 |
| --- | --- | --- |
| client 与封装 | `<填写：用哪个库、封装位置>` | `<填写>` |
| 基地址注入 | `<填写：环境变量名、构建时还是运行时>` | `<填写>` |
| 拦截器 | `<填写：鉴权头、错误翻译发生在哪>` | `<填写>` |
| 禁止事项 | `<填写：例如页面内写死环境 URL、绕过请求层 fetch>` | `<填写：现有封装或 lint 依据；无则写约定待落地>` |

## 3. 编码

- 只修改当前任务必要的代码，不附带无关重构或格式化。
- 遵循现有命名、模块边界和代码风格。
- 清理本次修改造成的无用内容，原有无关问题只报告。
- 先明确可验证目标，再实现并自测；不以改变规则掩盖失败。


### 3.1 语言与模块

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 语言与类型检查 | `<填写：TS / JS、严格档位>` | `<填写：tsconfig / 构建配置>` |
| 模块格式 | `<填写：ESM / CJS、路径别名>` | `<填写>` |
| 格式化与 lint | `<填写：工具与配置文件>` | `<填写>` |

### 3.2 命名

| 对象 | 约定 | 证据 |
| --- | --- | --- |
| 组件 | `<填写>` | `<填写：现有样例路径>` |
| 页面 | `<填写>` | `<填写>` |
| hook / 组合函数 | `<填写>` | `<填写>` |
| 工具与常量 | `<填写>` | `<填写>` |
| 测试文件 | `<填写：与源码同目录还是镜像目录>` | `<填写>` |

### 3.3 导入

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 别名 | `<填写>` | `<填写>` |
| 禁止跨层 | `<填写：哪些层不能被谁导入>` | `<填写>` |
| 循环依赖 | `<填写：如何检查、发现后怎么处理>` | `<填写>` |

### 3.4 错误处理

通用规则：用户可见文案与后端原始错误分离；错误码 / 鉴权头只在请求层或约定的契约层翻译一次，不散落在组件里。

| 场景 | 出口 | 证据 |
| --- | --- | --- |
| 请求失败（网络 / 超时） | `<填写：提示组件、重试入口>` | `<填写：错误组件或拦截器路径>` |
| 业务码失败 | `<填写：码表翻译位置、默认兜底>` | `<填写>` |
| 未登录 / 鉴权失效 | `<填写：跳转或弹窗、令牌刷新策略>` | `<填写>` |
| 渲染异常 | `<填写：错误边界位置；无则写无>` | `<填写>` |

## 4. 接口对接

类型、错误码、鉴权方式必须能指到契约或生成物；对不上就标「未核实」，不编造。契约的版本与变更义务在后端侧，见 [后端规范：技术规范](../后端规范/技术规范.md) §5；错误处理出口在本文 §3.4。

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 类型来源 | `<填写：手写 / OpenAPI 生成 / 共享包>` | `<填写：生成目录 / 共享类型路径>` |
| 错误码表 | `<填写：位置、如何同步>` | `<填写>` |
| 鉴权头 | `<填写：名称、谁注入>` | `<填写>` |
| mock 与联调 | `<填写：本地 mock 方式、是否依赖真实后端>` | `<填写>` |
