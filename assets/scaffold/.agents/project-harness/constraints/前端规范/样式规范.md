# 前端规范：样式规范

Last verified: <填写：YYYY-MM-DD>

使用时机：写或调整页面 UI、处理加载 / 空 / 错误三态、动设计令牌与响应式、定文案时读本文。

> 结构与请求层、编码与接口对接在 [技术规范](./技术规范.md)。

填写纪律：颜色、字号、间距优先走主题令牌；魔法值要能指回令牌或写明例外。本仓库无前端时，本文各节标不适用。

## 1. 主题与设计令牌

| 项 | 约定 | 证据 |
| --- | --- | --- |
| token 文件 | `<填写：位置、格式>` | `<填写>` |
| 暗色 / 主题切换 | `<填写：是否存在、如何切换>` | `<填写>` |
| 例外登记 | `<填写：硬编码色值允许的场合；无则禁止>` | `<填写>` |

## 2. 布局与响应式

| 项 | 约定 | 证据 |
| --- | --- | --- |
| 断点 | `<填写>` | `<填写：布局或 CSS 变量>` |
| 移动端 | `<填写：是否在范围内、最低支持>` | `<填写>` |
| 栅格与间距 | `<填写：档位来源>` | `<填写>` |

## 3. 加载、空、错误三态

每个会拉数的界面必须有加载、空、错三态；缺哪个就是缺陷，不在验收时补。

| 态 | 约定 | 证据 |
| --- | --- | --- |
| 加载态 | `<填写>` | `<填写>` |
| 空态（无数据 / 筛选为空） | `<填写>` | `<填写>` |
| 空态（无权限） | `<填写>` | `<填写>` |
| 错误态 | `<填写：可重试范围、是否暴露原始报文>` | `<填写>` |

## 4. 文案与语气

按钮 / 校验 / 空态用本产品语气。没有文案规范就写「跟随现有页面」，并指一个样例路径：`<填写>`。
