---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "目标用户、仓库范围或成功标准变化"
---

# 项目概述

现行叙述，不是规范。`owner` / `last_verified` / `verified_against` 只在有人认领或对照过仓库事实后填写；init 不猜负责人，不编造历史决策。核不到的项写「未核实」或「不适用」。

## 一句话

<与 `.agents/AGENTS.md` 的项目一句话保持一致，这里可多写一句边界。>

证据：`<填写：README / AGENTS / 产品说明路径>`

## 用户与场景

- 主用户：`<填写；无则未核实>`
- 典型场景：`<填写：谁、在什么入口、完成什么>`
- 明确不服务：`<填写>`

## 成功标准

怎样算这个仓库对用户有用。可验证，不写口号。

- `<填写：可观察结果 + 如何验证>`

## 范围

- 在本仓库内：`<填写：代码模块、页面、服务>`
- 依赖但不在本仓库：`<填写：下游系统、平台、SDK；无则无>`
- 明确不做：`<填写>`

证据：`<填写：模块根、功能地图行、依赖声明>`

## 入口

用户或调用方从哪进来。没有对外入口写「仅库 / 仅脚本」。

| 入口 | 给谁 | 路径或地址来源 | 证据 |
| --- | --- | --- | --- |
| `<填写：页面 / API / CLI / 任务>` |  | `<填写：路由、命令、配置项名；不抄真实 URL>` |  |

## 关键流

只写已核实的主路径，一至三条。细节落到架构页或领域页。

1. `<填写：触发 → 本仓库做什么 → 外部依赖 → 结果>`

## 外部依赖

只列真实依赖。配置只写名称与来源，不抄值。

| 依赖 | 用途 | 配置项名 | 来源 | 证据 |
| --- | --- | --- | --- | --- |
| `<填写或无>` |  | `<填写>` | `<填写：env / 配置中心 / 本地文件名>` |  |
