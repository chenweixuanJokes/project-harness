---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "该域模型、边界或持久化结构变化"
---

# <领域名>

落地时复制本模板并改名，不要为尚不存在的域预建空文档。术语与对象必须能指到代码或接口；猜出来的「当时为何这样建模」不要写。

## 职责

- 解决：`<填写>`
- 不解决：`<填写>`

## 术语

| 术语 | 含义 | 不要当成 |
| --- | --- | --- |
| `<填写>` |  |  |

## 核心对象

| 对象 | 含义 | 权威来源（代码或接口） |
| --- | --- | --- |
|  |  |  |

## 边界

- 上游：`<填写：谁驱动本域>`
- 下游：`<填写：本域调用谁>`
- 不跨过的线：`<填写：禁止直接碰的表 / 服务 / UI>`

## 关键限制

- `<填写：状态机、唯一性、审计、幂等；无则无>`

## 持久化与集成

只写本域真实用到的存储与外部系统。配置只留名称。

| 存储 / 系统 | 用途 | 配置项名 | 证据 |
| --- | --- | --- | --- |
| `<填写或无>` |  | `<填写>` |  |
