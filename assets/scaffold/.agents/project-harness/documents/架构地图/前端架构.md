---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "前端分层、路由框架、状态方案或构建入口变化"
---

# 前端架构

本仓库无前端时，把 `status` 改为 `deprecated`，正文写「不适用」。有前端但某层不存在则该节写「无」，不要按教程补齐。规则在 [前端规范](../../constraints/前端规范/技术规范.md)，本页只记结构事实。

## 分层

| 层 | 职责 | 路径 |
| --- | --- | --- |
| 入口 |  | `<填写>` |
| 路由 |  | `<填写>` |
| 页面 / 组件 |  | `<填写>` |
| 状态 |  | `<填写>` |
| 请求 |  | `<填写>` |
| 设计令牌 |  | `<填写或无>` |

## 入口与关键流

- 应用入口：`<填写：启动文件 / 根组件>`
- 用户可进的首条路径：`<填写：路由名，不写死环境域名>`

从打开应用到一次典型写操作：

1. `<填写：路由 → 页面 → 状态 / 请求层 → 后端契约>`

## 外部依赖

| 依赖 | 用途 | 配置项名 | 证据 |
| --- | --- | --- | --- |
| `<填写：BFF / 直连 API / 鉴权页；无则无>` |  | `<填写>` |  |

## 构建与产物

指向 [前端规范](../../constraints/前端规范/技术规范.md) 第 5 节，这里只记事实路径：

- 构建命令证据：`<填写>`
- 产物目录：`<填写或无>`
