---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "后端分层、模块拆分、数据存储或对外集成变化"
---

# 后端架构

本仓库无后端时，把 `status` 改为 `deprecated`，正文写「不适用」。公共组件是否存在以 [后端规范](../../constraints/后端规范/技术规范.md) 第 4 节为准；Kong / Eureka / Apollo 未出现在依赖里就不要写。

## 分层

| 层 | 职责 | 路径 |
| --- | --- | --- |
| 入口 |  | `<填写>` |
| 应用 / 用例 |  | `<填写>` |
| 领域 |  | `<填写>` |
| 持久化 |  | `<填写>` |
| 对外客户端 |  | `<填写或无>` |

## 模块

| 模块 | 职责 | 路径 | 领域页 |
| --- | --- | --- | --- |
| `<填写>` |  |  |  |

## 入口与关键流

- 进程入口：`<填写：main / 启动类 / 命令>`
- 对外接口入口：`<填写：路由或 RPC 清单路径>`

一条已核实写路径：

1. `<填写：接口 → 鉴权 → 领域 → 存储 / 下游 → 响应>`

## 接口与数据

只列权威位置，不在 Wiki 复制整本 schema。

| 项 | 权威来源 |
| --- | --- |
| 契约 | `<填写：OpenAPI / proto / 路由包>` |
| 主要数据模型 | `<填写：实体或表定义路径；细节回领域页>` |
| 迁移 | `<填写：目录或无>` |

## 集成

只列真实依赖的中间件与下游系统。

| 系统 | 用途 | 配置项名 | 证据 |
| --- | --- | --- | --- |
| `<填写或无>` |  | `<填写>` |  |

## 部署

进程模型与暴露端口的配置名写在这里，值不抄。细节回 [架构地图](./架构地图.md) 部署视图与 [构建发布与运维](../../constraints/后端规范/构建规范.md)。
