---
owner: "<填写>"
status: draft
last_verified: ""
verified_against: ""
refresh_trigger: "模块划分、调用关系或部署拓扑变化"
---

# 架构地图

结构事实。选型争议写入 [架构决策](../../constraints/架构决策/)，不在本页辩论。没有来源的「当时为何这样选」不要补。init 不猜 owner。

无前端或无后端时对应列 / 子页标不适用，并在子页把 `status` 改为 `deprecated`。

## 模块

| 模块 | 职责 | 代码路径 | 入口 |
| --- | --- | --- | --- |
|  |  |  |  |

证据：`<填写：源码根、构建模块清单>`

## 调用关系

用列表或短 ASCII 图表达谁调用谁。过期时先改本页，再改更细的前端 / 后端页。

```text
<填写：调用方 → 本模块 → 下游>
```

## 关键流

一条已核实的主路径即可；与 [项目概述](../项目概述.md) 对齐。

1. `<填写：入口 → 模块 → 存储 / 外部系统 → 响应>`

## 外部依赖

| 系统 | 方向 | 用途 | 证据 |
| --- | --- | --- | --- |
| `<填写或无>` | 入站 / 出站 |  |  |

## 部署视图

运行时有几份进程、对外暴露什么。没有独立部署则写「与开发进程相同」。环境名只写仓库已用的，不抄地址和密钥。

| 进程 / 产物 | 暴露什么 | 配置来源（只写名） | 证据 |
| --- | --- | --- | --- |
| `<填写>` |  |  |  |

排障入口见 [开发指南](../开发指南.md)；发布回滚见 [构建发布与运维](../../constraints/后端规范/构建规范.md)。
