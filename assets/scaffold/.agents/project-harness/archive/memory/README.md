# archive/

已合并的临时记忆原件。仅当原件无敏感内容时才移入本目录。归档时只改 frontmatter 的 `kind` 为 `archived-source`、`status` 为 `archived`；`created` / `updated` / 正文保持移入前原样。禁止 `rm`，禁止删除，禁止改写正文。用移动保留原件，不用删除。同名冲突时保留双方，新文件加 `-1` 后缀。发现密码、令牌、Cookie、私钥、完整连接串或个人信息时不得把该原件移入本目录。
